<form method="post" action="{{ path('app_back_programme_delete', {'id': programme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this program?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme.id) }}">
    <button type="submit" class="btn" style="background: none; border: none; padding: 0;">
        <i class="fas fa-trash-alt" style="color:red; font-size: 18px"></i>
    </button>
</form>