{% extends 'baseback.html.twig' %}

{% block title %}Adhérent{% endblock %}

{% block container %}

<div class="container d-flex justify-content-center" style="margin-top: 120px">
    <div class="row">
        <div class="col-12">
            <div class="row">
                <div class="col-lg-12 m-auto">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-header-2" style="margin-bottom: 20px">
                                <h3 class="text-center">Les informations de l'adhérent</h3>
                            </div>
                            <div >
    <form action="url_de_traitement" method="POST">
    <div class="form-group">
        <label for="id" style="text-decoration: underline">Id:</label>
        <label style="color: white; margin-left: 15px">{{ user.id }}</label>
    </div>
    <div class="form-group">
        <label for="nom" style="text-decoration: underline">Nom Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.nom }}</label>
    </div>
    <div class="form-group">
        <label for="prenom" style="text-decoration: underline">Prénom Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.prenom }}</label>
    </div>
    <div class="form-group">
        <label for="telephone" style="text-decoration: underline">Téléphone Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.telephone }}</label>
    </div>
    <div class="form-group">
        <label for="email" style="text-decoration: underline">Email Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.email }}</label>
    </div>
    <div class="form-group">
        <label for="cin" style="text-decoration: underline">CIN Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.cin }}</label>
    </div>
    <div class="form-group">
        <label for="sexe" style="text-decoration: underline">Sexe Adhérent:</label>
        <label style="color: white; margin-left: 15px">{{ user.sexe }}</label>
    </div>
    <!-- Vous pouvez ajouter d'autres champs ici si nécessaire -->
</form>
</div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    


    <a href="{{ path('app_back_adherent_index') }}">back to list</a>

    <a href="{{ path('app_back_adherent_edit', {'id': user.id}) }}">edit</a>

    {{ include('back/adherent/_delete_form.html.twig') }}
{% endblock %}












