{#{% extends 'base.html.twig' %}

{% block title %}Edit Affectation{% endblock %}

{% block body %}
    <h1>Edit Affectation</h1>

    {{ include('back/affectation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_affectation_index') }}">back to list</a>

    {{ include('back/affectation/_delete_form.html.twig') }}
{% endblock %}#}







{% extends 'baseback.html.twig' %}

{% block title %}Nouvelle affectation{% endblock %}

{% block container %}

    <div class="page-body">

    

                <!-- New Product Add Start -->
                <div class="container" style="margin-top: 80px">
                    <div class="row">
                        <div class="col-12">
                            <div class="row">
                                <div class="col-sm-8 m-auto">
                                    <div class="card" style="margin-top: 60px">
                                        <div class="card-body">
                                            <div class="card-header-2 justify-content-center" >
                                                <h4 class="text-center" style="margin-bottom: 50px; "><b>Les informations de l'affectation</b></h4>
                                            </div>

    {{ include('back/affectation/_form.html.twig', {'button_label': 'Modifier'}) }}

    {#<a href="{{ path('app_back_affectation_index') }}" style="text-decoration: underline; font-size: 17px;" class="mt-4">back to list</a>#}
    
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- New Product Add End -->
            </div>


    

    {#<a href="{{ path('app_back_affectation_index') }}">back to list</a>#}
{% endblock %}

