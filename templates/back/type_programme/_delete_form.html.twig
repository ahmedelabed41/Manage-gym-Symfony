<form method="post" action="{{ path('app_back_type_programme_delete', {'id': type_programme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this type ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_programme.id) }}">
    <button type="submit" class="btn" style="background: none; border: none; padding: 0;">
        <i class="fas fa-trash-alt" style="color:red; font-size: 18px"></i>
    </button>
</form>